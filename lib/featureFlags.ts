/**
 * Feature flags. Toggle modules without removing code.
 * EXECUTIVE: when false, nav/route/API for executive are hidden or return 404/redirect.
 */
export const FEATURES = {
  EXECUTIVE: false,
} as const;
